# Process-Monitor-terrible-edition-
**THIS SOFTWARE IS CREATED WITH AI** Program that lets you see suspicious processes on your computer


To run from src make sure to run `python -m PyInstaller --onefile --windowed --name ProcessMonitor process_monitor.py` to build the exe and to download the requirements run: `pip install -r requirements.txt`

